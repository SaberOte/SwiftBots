TODOS
=====
- Create makefile
- Finish adequate readme
- Handle exception not in runner, but in the message handler.
- In message handler create function which will execute methods itself
- Develop tasks/scheduler
- Problem with context. Make normal polymorphism
- Make creation of user folder (inject as python command)
- Create tests. Test at least chat and basic views
- Make REST api view
- Think about Models like a persistence storage
- Use colorama to write `warning` messages with yellow outline
- Stop using protected attributes outside classes
- Notify user when there's internal error
